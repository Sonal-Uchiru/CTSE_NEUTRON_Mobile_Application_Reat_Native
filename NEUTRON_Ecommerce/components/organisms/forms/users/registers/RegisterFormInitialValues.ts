import { RegisterModel } from "./RegisterFormModel";

export const RegisterInitialValues = {
  firstName: RegisterModel.firstName.initialValue,
  lastName: RegisterModel.lastName.initialValue,  
  contact: RegisterModel.contact.initialValue,  
  email: RegisterModel.email.initialValue,
  password: RegisterModel.password.initialValue,
  reEnterPassword: RegisterModel.reEnterPassword.initialValue
};
