import { ProfileModel } from "./ProfileFormModel";

export const ProfileInitialValues = {
  firstName: ProfileModel.firstName.initialValue,
  lastName: ProfileModel.lastName.initialValue,  
  contact: ProfileModel.contact.initialValue,  
  address: ProfileModel.address.initialValue,
};
